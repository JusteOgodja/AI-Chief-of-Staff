# Superhuman AI Chief of Staff — dependencies
# Python 3.11+

# Core
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Data & ingestion
pandas>=2.0.0
numpy>=1.24.0

# Graph
networkx>=3.0
pyvis>=0.3.0

# LLM (optional: only if you set LLM_PROVIDER=openai and OPENAI_API_KEY)
# Without them, the app uses heuristic extraction — no API key needed.
openai>=1.0.0
requests>=2.28.0

# API (choose one for MVP)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Dashboard (Streamlit for fast MVP)
streamlit>=1.28.0

# Optional: Neo4j if you switch from NetworkX
# neo4j>=5.0.0

# Optional: voice
# openai  # already includes Whisper/TTS

# Dev
pytest>=7.0.0
black>=23.0.0
